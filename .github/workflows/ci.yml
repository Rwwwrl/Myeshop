name: "eshop ci"

on:
  pull_request:
    branches: ["main"]

jobs:
  # demo:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
  #     - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
  #     - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
  #     - name: Check out repository code
  #       uses: actions/checkout@v4
  #     - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
  #     - run: echo "🖥️ The workflow is now ready to test your code on the runner."
  #     - name: List files in the repository
  #       run: ls ${{ github.workspace }}
  #     - run: echo "🍏 This job's status is ${{ job.status }}."
  #     - run: echo "current branch is ${{ github.head_ref }}"

  flake8_linter:
    runs-on: ubuntu-latest
    steps:
      - name: checkout to repository code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: setup linter
        run: pip install -r deploy/ci/linter/flake8_requirements.txt
      - name: run linter
        run: flake8 --config=setup.cfg $(git diff origin/${{ github.event.pull_request.base.ref }} --name-only --diff-filter=d | grep -E '.+\.py$')

  pytest_contract_usage:
    runs-on: ubuntu-latest
    steps:
      - name: checkout to repository code
        uses: actions/checkout@v4
      - name: setup project
        run: ./deploy/ci/build_project.sh && pip install -r requirements/for_run_tests.txt
      - name: run pytest
        run: pytest -c pytest.ini -m cqrs_contract_usage

  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: checkout to repository code
        uses: actions/checkout@v4
      - name: setup project
        run: ./deploy/ci/build_project.sh && pip install -r requirements/for_run_tests.txt
      - name: run pytest
        run: pytest -c pytest.ini
